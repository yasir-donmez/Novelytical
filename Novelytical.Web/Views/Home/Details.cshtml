@model Novelytical.Application.DTOs.NovelDetailDto

@{
    ViewData["Title"] = Model.Title;
}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-4">
            @if (!string.IsNullOrEmpty(Model.CoverUrl))
            {
                <img src="@Model.CoverUrl" class="img-fluid rounded shadow-lg w-100" alt="@Model.Title">
            }
            else
            {
                <div class="bg-secondary text-white d-flex align-items-center justify-content-center rounded shadow-lg" style="height: 400px;">
                    <h3>Resim Yok</h3>
                </div>
            }
            
            <div class="mt-3 d-grid">
                <a href="@Model.SourceUrl" target="_blank" class="btn btn-outline-primary">
                    🌍 Royal Road'da Gör
                </a>
            </div>
        </div>

        <div class="col-md-8">
            <h1 class="display-4 fw-bold">@Model.Title</h1>
            <h4 class="text-muted mb-4">✍️ @Model.Author</h4>

            <div class="d-flex gap-3 mb-4">
                <span class="badge bg-primary fs-6 p-2">⭐ @Model.Rating</span>
                <span class="badge bg-secondary fs-6 p-2">📄 @Model.ChapterCount Bölüm</span>
                <span class="badge bg-success fs-6 p-2">📅 @Model.LastUpdated.ToString("dd MMM yyyy")</span>
            </div>

            <div class="mb-4">
                @if (Model.Tags != null && Model.Tags.Any())
                {
                    @foreach (var tag in Model.Tags)
                    {
                        <span class="badge bg-info text-dark me-1">@tag</span>
                    }
                }
            </div>

            <hr />

            <h3 class="mt-4">📖 Özet</h3>
            <p class="lead" style="white-space: pre-line;">
                @Model.Description
            </p>

            <div class="mt-5">
                <a asp-action="Index" class="btn btn-secondary">⬅️ Listeye Dön</a>
            </div>
        </div>
    </div>
</div>