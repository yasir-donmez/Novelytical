services:
  novelytical-postgres:
    image: pgvector/pgvector:pg16
    container_name: novelytical-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-NovelyticalDb}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "127.0.0.1:5432:5432" # 🔒 Localhost access only
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - novelytical-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always

  seq:
    image: datalust/seq:latest
    container_name: novelytical-seq
    environment:
      ACCEPT_EULA: Y
    ports:
      - "127.0.0.1:5341:5341" # 🔒 Localhost access only
      - "127.0.0.1:8888:80" # 🔒 Localhost access only
    volumes:
      - seqdata:/data
      - seqdata:/data
    restart: always

  novelytical-redis:
    image: redis:alpine
    container_name: novelytical-redis
    ports:
      - "127.0.0.1:6379:6379" # 🔒 Localhost access only
    restart: always

  novelytical-web:
    build:
      context: .
      dockerfile: Novelytical.Web/Dockerfile
    container_name: novelytical-web
    environment:
      - ConnectionStrings__DefaultConnection=Host=novelytical-postgres;Database=${POSTGRES_DB:-NovelyticalDb};Username=${POSTGRES_USER:-postgres};Password=${POSTGRES_PASSWORD}
      - ConnectionStrings__Redis=novelytical-redis:6379
      - Seq__ServerUrl=${SEQ_SERVER_URL:-http://seq:5341}
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "5050:8080"
    depends_on:
      novelytical-postgres:
        condition: service_healthy
      seq:
        condition: service_started
    networks:
      - novelytical-network
    restart: on-failure

  novelytical-worker:
    build:
      context: .
      dockerfile: Novelytical.Worker/Dockerfile
    container_name: novelytical-worker
    environment:
      - ConnectionStrings__DefaultConnection=Host=novelytical-postgres;Database=${POSTGRES_DB:-NovelyticalDb};Username=${POSTGRES_USER:-postgres};Password=${POSTGRES_PASSWORD}
      - ConnectionStrings__Redis=novelytical-redis:6379
      - Seq__ServerUrl=${SEQ_SERVER_URL:-http://seq:5341}
      - DOTNET_ENVIRONMENT=Development
    depends_on:
      novelytical-postgres:
        condition: service_healthy
      seq:
        condition: service_started
    networks:
      - novelytical-network
    restart: on-failure

  novelytical-client:
    build:
      context: ./novelytical-client
      dockerfile: Dockerfile
    container_name: novelytical-client
    environment:
      - API_URL=http://novelytical-web:8080 # 🐳 Internal Docker Network
      - NEXT_PUBLIC_API_URL=http://localhost:5050 # 🌍 Browser Access (Client Side)
    ports:
      - "3000:3000"
    networks:
      - novelytical-network
    depends_on:
      - novelytical-web
    restart: on-failure

networks:
  novelytical-network:
    driver: bridge

volumes:
  postgres_data:
  seqdata:
